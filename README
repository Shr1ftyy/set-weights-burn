This script sets max weight to the owner coldkey for sn26 - thereby burning the alpha emitted to it.
This will be used until the subnet's code is ready for mainnet launch.

Running:
pip install -r requirements.txt
python set_weights.py --wallet-name COLDKEY_NAME --hotkey-name HOTKEY_NAME

done :)
